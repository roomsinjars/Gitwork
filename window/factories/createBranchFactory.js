var NodeGit = require("nodegit");

app.factory('createBranchFactory', function(){
	return{
		
		// Branch.create($scope.repository, branch_name, target, force, signature, log_message).then(function(reference) {
  // Use reference
	}
})

