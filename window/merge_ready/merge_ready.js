app.config(function($stateProvider, $urlRouterProvider){

    $stateProvider
        .state('merge_ready', {
            url: '/merge_ready',
            templateUrl: 'window/merge_ready/merge_ready.html'
        })
});
